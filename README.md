At first: npm install -D typescript@5.9.2

Then: npm install -D ts-node@latest

npx tsc --init for config tsconfig.json

npm install ts-node ts-node-dev @types/node @types/express @types/cors

npx tsc --init
